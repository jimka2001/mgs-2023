# define a function which takes a single argument
#def hello(name):
   # print("hello " + name)

# call the function with an argument
#hello("gertrude")

def sequence(nu):
    print(f"the sequence is, {nu-1} {nu} {nu+1}")

sequence("4")